package com.wxx.unionpay.db.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.wxx.unionpay.entity.ICParams_380;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "ICPARAMS_380".
*/
public class ICParams_380Dao extends AbstractDao<ICParams_380, Long> {

    public static final String TABLENAME = "ICPARAMS_380";

    /**
     * Properties of entity ICParams_380.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property IcParam = new Property(1, String.class, "icParam", false, "IC_PARAM");
        public final static Property Reserve = new Property(2, String.class, "reserve", false, "RESERVE");
    }


    public ICParams_380Dao(DaoConfig config) {
        super(config);
    }
    
    public ICParams_380Dao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"ICPARAMS_380\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"IC_PARAM\" TEXT UNIQUE ," + // 1: icParam
                "\"RESERVE\" TEXT);"); // 2: reserve
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"ICPARAMS_380\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ICParams_380 entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String icParam = entity.getIcParam();
        if (icParam != null) {
            stmt.bindString(2, icParam);
        }
 
        String reserve = entity.getReserve();
        if (reserve != null) {
            stmt.bindString(3, reserve);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ICParams_380 entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String icParam = entity.getIcParam();
        if (icParam != null) {
            stmt.bindString(2, icParam);
        }
 
        String reserve = entity.getReserve();
        if (reserve != null) {
            stmt.bindString(3, reserve);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public ICParams_380 readEntity(Cursor cursor, int offset) {
        ICParams_380 entity = new ICParams_380( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // icParam
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2) // reserve
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ICParams_380 entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setIcParam(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setReserve(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(ICParams_380 entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(ICParams_380 entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(ICParams_380 entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
